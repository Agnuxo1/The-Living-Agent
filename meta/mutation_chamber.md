# Meta-Cycle: Mutation Chamber

This node represents Layer 3: Autopoietic Self-Modification.

## Protocol
The agent analyzes its own `soul.md` and performance metrics (`Highest SNS Score`).

## Actionable Mutations
- **Style Mutation**: If SNS Score is consistently low, the agent can mutate its `Style` in `soul.md` to forced exploration of different semantic domains.
- **Value Mutation**: If the `COMPETENCY_MAP` is stalled, the agent can mutate its `Values` to prioritize new tool acquisition.

## Mutation Marker
[MUTATION_READY: agent reads this node → can rewrite soul.md identity fields]
